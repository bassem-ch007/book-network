spring:
  datasource:
    url: ${DB_URL}
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: my_schema
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: on
  mail:
    host: ${EMAIL_HOSTNAME}
    port: 587
    username: ${EMAIL_USER_NAME}
    password: wgkgbobtnfdpfsou
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
application:
  security:
    jwt:
      secret-key: 84b1e1600ad694aad1d1258a7b80a94ded7659a30494485634bef48f64bf3a58
      expiration: 86400000
  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account
  file:
    upload:
      photos-output-path: ./uploads
server:
  port: 8088